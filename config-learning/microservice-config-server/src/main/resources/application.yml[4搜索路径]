server:
  port: 8080
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/davidsky11/config-server-repo   # 公用
          search-paths:  # 搜索路径
            - foo   # foo路径
            - bar   # bar路径

